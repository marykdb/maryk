# Maryk CLI docs

This folder collects CLI usage notes and command reference material.

Start with:
- `commands.md` for the command list, options, and interactive flows.

If you are browsing from the website, see the CLI overview at `/cli/`.

package maryk.core.properties.enum

import maryk.core.properties.PropertyDefinitions

/** Defines an enum case for an embed so types can be strongly resolved */
interface EmbedTypeCase<E: IndexedEnum, P: PropertyDefinitions>

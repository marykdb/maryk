apply plugin: 'maven-publish'

kotlin {
    sourceSets {
        all {
            languageSettings {
                languageVersion = '1.7'
                apiVersion = '1.7'
                optIn("kotlin.RequiresOptIn")
                progressiveMode = true
            }
        }
    }
}

apply plugin: 'maven-publish'

kotlin {
    sourceSets {
        all {
            languageSettings {
                languageVersion = '1.6'
                apiVersion = '1.6'
                optIn("kotlin.RequiresOptIn")
                progressiveMode = true
            }
        }
    }
}

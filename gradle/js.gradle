// Gradle script to setup a JS multi-platform project
kotlin {
    js {
        compilations.all {
            kotlinOptions {
                freeCompilerArgs += "-Xopt-in=kotlin.RequiresOptIn"
            }
        }
        browser {}
        nodejs {}
    }
}

kotlinNodeJs {
    nodeVersion = '16.13.0'
}

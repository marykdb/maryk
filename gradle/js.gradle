// Gradle script to setup a JS multi-platform project
kotlin {
    js(IR) {
        compilations.all {
            kotlinOptions {
                freeCompilerArgs += "-opt-in=kotlin.RequiresOptIn"
            }
        }
        browser {}
        nodejs {}
    }
}

kotlinNodeJs {
    nodeVersion = '16.13.0'
}

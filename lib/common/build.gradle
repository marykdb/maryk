apply {
    plugin("kotlin-platform-common")
}

evaluationDependsOn(':test-common')

dependencies {
    compile("org.jetbrains.kotlin:kotlin-stdlib-common:$kotlinVersion")

    testCompile(project(":test-common"))
}
